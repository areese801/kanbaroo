[project]
name = "kanberoo-core"
version = "0.1.0"
description = "Kanberoo core: models, schemas, business logic, migrations"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Adam Reese" }]
dependencies = [
    "sqlalchemy>=2.0",
    "alembic>=1.13",
    "pydantic>=2.7",
    "uuid-utils>=0.9",
    "pyarrow>=15",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kanberoo_core"]

[tool.hatch.build.targets.wheel.force-include]
"alembic" = "kanberoo_core/alembic"

[project]
name = "kanberoo-web"
version = "0.0.1"
description = "Placeholder. Reserved for the Kanberoo phase 2 web UI. Not yet released."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Adam Reese" }]
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kanberoo_web"]

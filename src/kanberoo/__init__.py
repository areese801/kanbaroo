"""
Kanberoo: a kanban-style issue tracker.

This is the top-level meta-package. Install the optional ``all`` extra
for everything::

    pip install kanberoo[all]

Or install individual sub-packages directly::

    pip install kanberoo-core
    pip install kanberoo-api
    pip install kanberoo-tui
    pip install kanberoo-cli
    pip install kanberoo-mcp
"""

__version__ = "0.1.0"
